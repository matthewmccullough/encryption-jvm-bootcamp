To run this demo, from one prompt type:

    servefile?.sh

From another prompt, type the matching retrieval:

    getfile?.sh

The file echoed to the screen will be the contents of `samplemessage.txt`

To examine the keystore used for SSL or TLS:

    keytool -list -keystore testkeys

No password is required for listing the keystore.